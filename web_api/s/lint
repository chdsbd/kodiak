#!/usr/bin/env bash
set -ex

# format code
if [[ $CI ]]; then
    ./.venv/bin/ruff format --check .
    ./.venv/bin/ruff .
else
    ./.venv/bin/ruff format .
    ./.venv/bin/ruff . --fix
fi

# type check code
./.venv/bin/mypy .
