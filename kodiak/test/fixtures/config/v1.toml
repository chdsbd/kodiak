version = 1
[merge]
whitelist = ["automerge"]
blacklist = ["wip", "block-merge"]
method = "squash"
delete_branch_on_merge = false

[merge.message]
title = "github_default"
body = "github_default"

[update]
blacklist = ["no-update"]
